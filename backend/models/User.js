export class User {
    constructor(id, name, age, weight, height, bmi, goal, activityLevel) {
      this.id = id;
      this.name = name;
      this.age = age;
      this.weight = weight;
      this.height = height;
      this.bmi = bmi;
      this.goal = goal;
      this.activityLevel = activityLevel;
    }
  }